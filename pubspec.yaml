name: pokedex_app
description: Pokédex App by Silph Co. x Dynamo

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  cached_network_image: ^3.3.0
  flutter_staggered_animations: ^1.1.1
  shimmer: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/